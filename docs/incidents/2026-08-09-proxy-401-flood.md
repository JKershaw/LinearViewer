# Incident — sustained Linear 401 flood on the proxy lane

**Date:** 2026-08-08 – 2026-08-09
**Duration:** onset ≤ 08-08 10:53 UTC, recovered ≈ 08-09 08:43 UTC (**≈12 h**), with a
tail of agent-visible 401s to 08-09 09:57:32
**Severity:** Degraded — autopilot agents could not read or write Linear. No data loss,
no bad writes. ~12 h of pipeline stall across the LIN-1929 and LIN-1887/1890 arcs.
**Status:** Root cause **NOT** identified. Amplifier identified; fix built and held as
an unmerged draft (PR #1099) pending evidence. Service confirmed restored — all active
workers healthy, no auth errors in any current stream.

> **All times UTC.** This document was first written from the *tail* of the
> incident (08-09 09:26–09:57) and framed that half hour as the whole event. A
> fleet-wide sweep of in-flight session feedback later established a ≈12 h
> outage beginning the previous day. Both the duration and the onset in the
> original revision were wrong; see *Corrections*.

---

## What happened

For roughly twelve hours, every Harbour call that touched Linear returned
`401 AUTHENTICATION_ERROR` while Harbour's own endpoints (`/instructions`, `/dispatch`,
the agent-status channel) served 200 throughout. GitHub credentials were explicitly
unaffected. Dispatched agents could not read or write Linear; three separate sessions
detected the fault independently, parked cleanly, and preserved verified state.

The visible symptom that triggered this investigation was the **tail**: a wall of ~1/sec
401s on 08-09 between 09:26:31 and 09:57:32, after the credential had apparently already
recovered at ≈08:43. That tail is the interesting part, and it is what the findings
below are about — a *recovered* workspace still serving a dead credential to some
callers.

```
Proxy /issue error: Authentication required, not authenticated:
  {"response":{"errors":[{"extensions":{"code":"AUTHENTICATION_ERROR","statusCode":401,
   "userPresentableMessage":"You need to authenticate to access this operation."}}],
   "status":401}, "request":{"variables":{"id":"266f0841-…"}}}
```

The critical property: **this was not a resolution failure.** `resolveWorkspaceAccess`
succeeded, returned a token it believed live, routed to the Linear provider, and
*Linear* rejected the credential. A `WORKSPACE_NOT_CONNECTED` 503 would have meant the
opposite — that no token was found. Most of the investigation time was lost to chasing
the wrong branch, because nothing in the logs distinguished them at a glance.

## Timeline (UTC)

Clock provenance is called out per row, because conflating it is what produced the
original error. "System" = a UTC timestamp emitted by Harbour or its API.

| Time (UTC) | Clock | Event |
|---|---|---|
| **08-07** | — | Jira **Basic-auth** binding linked and validated in production. Carries `tokenExpiresAt: Number.MAX_SAFE_INTEGER` (no expiry) |
| **08-08 10:53** | session feedback | **Onset.** Session `1d920362` (LIN-1929) reports `GET /api/proxy/me` → 401, confirmed on retry. Parks with PR #1087 green |
| 08-08 15:30 | session feedback | Session `1f5e6f81` (Jira arc): same signature, re-verified fresh. Parks PR #1088 approved-but-unmerged |
| 08-08 22:55 | session feedback | Stack-walk autopilot `966c63e1`: 401 on all Linear reads **and** writes. Parks LIN-1890 review→close-out and LIN-1948 |
| **08-09 ≈08:43** | probe | **Recovery.** A fresh token pulls live Linear data on every call. Consistent with an overnight re-link/refresh |
| 08-09 09:23:29 | system | A `review` dispatch for LIN-1970 is queued |
| 08-09 09:26:31 | system | **First error** — `resolveWorkspaceTitles error: Authentication required, not authenticated` |
| 08-09 09:30 onward | system | Sustained `Proxy /issue error` + `Linear auth error (HTTP 401)`, ~1/sec |
| 08-09 09:30–09:40 | screenshot (local→UTC) | Solid red. Four distinct issue UUIDs failing in rotation |
| 08-09 09:50:23 | system | Sample captured (issue `266f0841-…`) |
| 08-09 09:57:32 | system | **Last error** — `Proxy /me error` (the `viewer` credential probe). Flood ends |
| unknown | — | Workspace removed and re-added by the operator. Not timestamped; see below |
| — | — | Operator's own proxy token reads the *same* failing issue ids at **200** |

**Why the tail ended.** The final call is `/me` — the `viewer` query, the canonical "is
my credential still valid?" probe. The pattern is an agent 401ing on reads, probing
`/me`, seeing 401, concluding its credential is dead, and stopping. **The callers gave
up; nothing in Harbour repaired the credential.** The queue corroborates it: 22 `wake`
items sat at `feedbackCount: 0`, never claimed.

**Why the tail existed at all is the sharpest question this incident poses.** The
credential had recovered at ≈08:43 — an operator token pulled live Linear data on every
call, including the exact issue ids (LIN-1542 … LIN-1545) that were 401ing for agents at
the same moment. So a *recovered* workspace was still handing a dead credential to some
callers 45 minutes later.

The mechanism that fits is owner-scoped selection over stale session rows. Selection is
per-`(urlKey, ownerAccountId)` and picks by **maximum `tokenExpiresAt`**, so a re-link
that writes a fresh credential into one session row does not retire the dead one in
another. An operator resolving the fresh row sees 200; an agent whose owner scope
resolves the stale row sees 401 — indefinitely, because the stale token's recorded
expiry is still in the future (finding 2) and nothing evicts it (findings 3–4).

If that is right, the tail is not a second fault. It is the first fault's credential
surviving its own fix, and findings 2–4 are the reason. It also promotes follow-up 4
(max-expiry selection) from cleanup to probable-cause-adjacent. **Unconfirmed** — it
predicts distinct `credentialFingerprint`s for operator vs. agent callers on the same
workspace, which the diagnostics now record and which no evidence yet shows.

**The re-add is not timestamped**, so it cannot be ordered against 09:57:32. If it
landed just before, it — not the agents giving up — may be what ended the tail. The two
are distinguishable in the audit log (`/api/proxy/events`) and were not separated.

Deployed commit through the 08-09 tail: `fc0a4c84`; no deploy occurred in that window.
The only change authored that day (`717f2f7`, the `write:jira-work` scope) was never
deployed and is not implicated. Deploy activity across the 08-08 onset was not checked
and should not be assumed quiet.

## What is established

1. **The workspace's Linear credential was never globally dead.** The same issue ids
   that 401'd for the agents (LIN-1542 … LIN-1545) returned 200 for an operator token
   in the same window, on the same endpoint. The failure was **credential-specific**.
2. **Token selection admits revoked credentials.** `selectOwnerWorkspaceToken`
   (`lib/workspace-token-resolver.js:113`) admits any token whose `tokenExpiresAt` is
   more than five minutes in the future. A token *revoked upstream* is indistinguishable
   from a healthy one until the provider answers 401.
3. **There is no self-heal on the headless lane.** `evictWorkspaceToken` is called from
   zero places in `routes/proxy.js`. `graphqlErrorStatus` maps the upstream 401 → 401
   (`routes/proxy.js:963`), returns it, and stops. The stored credential and its 30s
   cache entry are untouched, and the next poll re-serves the identical dead credential.
   `handleUnauthorizedError` — which *does* refresh, re-mint, or prompt a re-link — is
   on the session/web path only. A headless 401 teaches the system nothing.
4. **Refresh-on-resolve cannot fire for this failure mode.** It is gated on
   `!selected.token` (`server.js:1791`), reachable only when *no* token resolved. A
   dead-but-unexpired token sails past it.

(3) and (4) together fully explain the *persistence*: once a stored credential is
dead-but-unexpired, the lane re-serves it on every request, for as long as anything
keeps polling. That is the solid red wall, and it is why nothing the operator did from a
phone stopped it.

Stated precisely, because an earlier revision overreached here: this does **not** mean
the errors would have continued forever. They stopped at 09:57:32 — because the callers
stopped, not because Harbour healed. The credential was never repaired, evicted, or
refreshed by anything in the system. The failure mode is therefore not "an unbounded
error stream" but "agents are taken offline and the work silently stalls", which is
harder to notice, not easier.

## What is NOT established

**The trigger.** Nothing above says what killed the credential in the first place.

One **open, unconfirmed** candidate: `linkProvider` (`lib/workspace.js:312-327`)
mirrors a newly-linked provider's token *and its expiry* onto the workspace's scalar
fields whenever `workspace.provider` is unset — which is the legacy state for a Linear
workspace, since absent-provider is read as Linear throughout. Selection is then by
**maximum `tokenExpiresAt`, not recency**, so a mirrored never-expires sentinel
(`Number.MAX_SAFE_INTEGER`, used by Jira Basic and GitHub-family bindings) would win
selection permanently — unbeatable by a token refresh, and unbeatable by re-connecting
the workspace, since a fresh Linear token's `now+24h` always loses.

**This hypothesis is weaker than the first revision of this document claimed**, and the
correction matters more than the hypothesis:

- *The timing evidence was cross-clock.* It was originally stated as "Jira linked at
  09:22, first 401 at 09:26:31 — four minutes apart." The 09:22 was a **local** time
  mentioned in conversation (and about the current time, not the link); the 09:26:31 is
  **UTC** from the logs. Converted, the link was roughly an hour *before* onset, not
  four minutes. **The correlation the hypothesis was built on does not hold** — and the
  real onset turned out to be the previous day, so the 08-09 Jira OAuth work cannot be
  the trigger at all.
- *But a DIFFERENT Jira link fits the corrected onset better than the discarded one
  ever fit the wrong one.* The Jira **Basic-auth** binding was linked and
  production-validated on **08-07**, and a Basic binding carries exactly the
  `Number.MAX_SAFE_INTEGER` sentinel this mechanism needs. Sequence: the mirror lands
  08-07; the healthy Linear token keeps winning selection on its own later expiry until
  it lapses roughly 24 h later; selection then falls to the never-expiring row and every
  Linear call 401s from **08-08 ~10:53**. The ~24 h delay between link and onset is a
  *prediction* of the mechanism rather than an awkwardness for it. Reached only after
  the onset was corrected — which is the argument for fixing a timeline before
  theorising over it.
- *The counter-evidence is unresolved.* The same code path also sets
  `workspace.provider` to `jira`, which should route reads to JiraProvider. The logs
  show a **Linear** GraphQL query throughout. Either this is not the mechanism, or the
  provider field and the token diverged across session rows.

What *does* survive is the one same-clock comparison: the LIN-1970 `review` dispatch at
**09:23:29** and the first error at **09:26:31**, three minutes apart, both system
timestamps. That is a genuine correlation — though a dispatch starting an agent that
then begins polling is an equally good explanation for it as anything causal, since the
agent's first read is simply the first time the dead credential would be exercised.

The hypothesis is therefore **open, not leading.** It is retained because the code path
is real and reachable and the diagnostics now test for it directly, not because the
evidence points at it.

## Was the 08-09 tail a second, separate fault?

A fleet sweep classified the 08-09 09:25–09:57 window as a **distinct** fault: session
`0be254af`'s local credential-broker (`127.0.0.1:56431`) 401ing on every call including
`/me`, with its previous port (56498) refusing connections — read as the broker being
restarted mid-session with credential injection returning broken.

The window matches this document's tail to within ~90 seconds at both ends, so it is
certainly the same event. **The log signature, however, is server-side, not broker-side.**
Harbour logged `Proxy /issue error` and `Proxy /me error` carrying *Linear GraphQL
request bodies*. For those lines to exist at all, the caller's bearer token must have
authenticated, a workspace credential must have resolved, and **Linear** must have
rejected it. A broken broker injection fails earlier — at `authenticateProxyToken` —
and produces no `Proxy /*` line.

So the most likely reading is that the two are **one fault, diagnosed from two sides**:
the broker did restart (the refused port is real, independent evidence), and what it
reached on the far side was a stale Linear credential.

The reason the worker called it a broker fault is the important part, and it is not a
mistake anyone could have avoided at the time: **from inside the sandbox the two are
indistinguishable.** A dead workspace credential and a broken local injection both
return 401 on every call including `/me`. There was no field anywhere in the response or
the logs that separated them.

That is exactly the ambiguity the `stage` field in the shipped diagnostics resolves —
`proxy-token` (the caller's bearer token was refused) vs. `provider-lane` (Harbour's
stored credential was refused upstream) — which would have settled this on the first
line rather than after a fleet sweep. Unconfirmed until a recurrence prints one.

## A silent data-integrity finding

Worth recording separately because it is a class of bug, not an instance. In session
`0be254af`, **1,193 of 1,200 issue reads failed** with "detail fetch failed", and the
script's `tolerate: true` retry policy swallowed every one of them as a *skip*. The run
would have published a baseline artifact that was quietly ~90% empty while reporting
success. The worker caught it and killed the run, so no corrupt artifact was produced.

The general defect: a retry/tolerate policy written for *transient* failures treated a
uniform *authentication* failure as tolerable noise. Auth failures are never
tolerable-by-skipping — they are total, they do not improve with retries, and a policy
that degrades them to skips converts a loud outage into a silent corruption. Filed as
follow-up 6.

Three hypotheses were **ruled out** by reading the code and should not be re-investigated:
- Global Linear outage or workspace-wide revocation — refuted by the 200s above.
- Durable-store cross-provider overwrite — `OwnerCredentialStore.put` partitions
  correctly on `(accountId, urlKey, provider)`.
- `linkProvider` clobbering a workspace already marked `linear` — the `isActive` guard
  blocks it. (The *unset*-provider hole above is a different, still-open case.)

## Why it took so long

The logs could not answer any of the questions that would have identified this:

- Which credential did this call use? *(Unanswerable — nothing identified one.)*
- Where did it come from — cache, session scan, or refresh?
- What expiry did the server believe it had?
- Was the credential even the right *shape* for the resolved provider?
- Was this a bad **caller** token or a bad **workspace** credential? Both are 401 with
  opposite remedies, and both logged identically.

A secondary cost: early in the incident the on-call agent probed production with **its
own** working token, got 200s across the board, and reported the system healthy. That
was a bad test — it measured the one credential known to work while the failing ones
went unmeasured — and it delayed escalation. Probing with a credential that is not the
failing credential proves nothing.

## Fix shipped

Diagnostics only. `lib/credential-diagnostics.js` (pure, secret-safe) plus wiring at the
proxy lane's two chokepoints — the resolution site (`resolveProviderAccess`) records a
descriptor, the status site (`logEvent`) emits it on any 401.

Each 401 now emits one `[credential-rejected]` line carrying:

| Field | Answers |
|---|---|
| `stage` | caller's bearer token vs. stored workspace credential |
| `credentialFingerprint` | *which* credential — correlatable across replicas, one-way |
| `credentialSource` | `cache` / `session-scan` / `refresh-on-resolve` |
| `expiryKind` + `msUntilExpiry` | `finite` / **`sentinel`** / `absent` |
| `credentialShape` + `shapeMismatch` | a cross-provider credential, flagged on sight |
| `provider` | distinguishes explicitly-`linear` from **unset** (the clobber-exposed case) |
| `proxyTokenId` / `ownerAccountId` | which agent, whose owner stamp |

`shapeMismatch` and `expiryKind: 'sentinel'` are the two fields that would confirm or
kill the `linkProvider` hypothesis on the next occurrence, without any further
investigation.

No token bytes are logged, in any field or shape — asserted over the whole serialized
payload so a field added later cannot quietly leak. Deliberately not rate-limited: a
flood of identical fingerprints is precisely the evidence that one stuck credential is
being re-served, and throttling would erase it.

## Follow-ups — NOT done here

1. **No 401 invalidation on the headless lane.** The amplifier is diagnosed but not
   fixed. A provider 401 should evict the cached credential and force re-resolution
   (with backoff), so a dead credential cannot be re-served indefinitely. *This is the
   fix that would have bounded the incident to seconds.*
2. **Selection admits revoked credentials.** Unexpired ≠ valid. Worth recording a
   last-known-rejected marker so selection can skip a credential the provider just
   refused.
3. **`linkProvider`'s unset-provider mirror.** Confirm or refute the clobber against
   stored data, then close the hole.
4. **Sentinel expiry beats everything, permanently.** Max-expiry selection means a
   never-expires credential can never be displaced by a healthy one. Selection should
   probably prefer a provider-matched credential over a merely longer-lived one.
   **Raised in priority:** this is now the leading explanation for the 08-09 tail (a
   recovered workspace still serving a dead credential), not merely a latent hazard.
5. **`resolveWorkspaceForTitles`** (`lib/workspace-title-resolver.js`) scans every
   session doc in the database, is owner-blind and provider-blind, and swallows its
   errors. It produced the first symptom of the 08-09 tail. Not touched here.
6. **`tolerate: true` swallows authentication failures as skips.** See *A silent
   data-integrity finding*. A retry policy written for transient errors degraded a total
   auth outage into 1,193 silent skips and would have published a ~90%-empty baseline as
   a success. Auth failures should be non-tolerable by construction.
7. **A worker cannot tell a broker fault from a server-side credential fault.** The
   `stage` field separates them in Harbour's logs, but nothing surfaces the distinction
   to the agent making the call — so a worker will keep diagnosing one as the other. See
   *Was the 08-09 tail a second, separate fault?*
8. **Stale session rows are never retired on re-link.** A re-link writes a fresh
   credential into one session row without retiring the dead one in others, so different
   owners resolve different credentials for the same workspace indefinitely.

## Corrections to this document

Recorded rather than silently edited, because both errors were reasoning errors in the
investigation, not typos — and the second one shaped a conclusion.

1. **Duration was overstated as ≈75 min; it was ≈31 min** (09:26:31 – 09:57:32 UTC).
   A Railway screenshot timestamped 10:30–10:40 was read as UTC and treated as the tail
   of the incident. It was **local** time (BST, UTC+1) — 09:30–09:40 UTC — which sits
   in the *middle* of the window, not past its end.

2. **The leading hypothesis rested on a cross-clock correlation.** "Jira linked at
   09:22, first 401 at 09:26:31" compared a local time against a UTC one. Converted,
   the gap is about an hour, not four minutes. The hypothesis was downgraded from
   *leading* to *open* once this surfaced.

3. **"Would not have stopped on its own" was too strong.** The lane re-serves a dead
   credential for as long as anything polls; it does not generate errors forever. The
   flood ended when the callers gave up.

4. **The onset was wrong by ~22 hours, and the incident length by ~11.5.** This
   document was written from the tail (08-09 09:26–09:57) and framed it as the whole
   event. The real onset was ≤ 08-08 10:53, established only by a fleet sweep of
   in-flight session feedback. Railway logs alone could not have shown it: the sessions
   that detected the fault first recorded it in dispatch feedback and on the
   agent-status channel — both Harbour-served, both unaffected — and neither was
   consulted here.

5. **The tail was investigated as though it were the incident.** Every finding under
   *What is established* was derived from a half-hour window that turned out to be the
   *recovery artefact* of a twelve-hour outage. The findings survive — they explain why
   a recovered workspace kept serving a dead credential — but they were never a
   root-cause account of the outage, and the original document implied they were.

The general lesson is narrow and worth keeping: **never compare a human-reported time
to a log timestamp without converting first.** Two of the three errors above are the
same mistake. The timeline table now records clock provenance per row so the comparison
cannot be made implicitly again.

There is a second-order lesson about the first one. The incorrect duration was not
load-bearing on its own — but it was cited as supporting evidence for the hypothesis in
the same breath as the cross-clock link time, which made a weakly-supported theory read
as a well-corroborated one. Wrong facts in an incident log do not stay decorative; they
get recruited.

A second lesson, from the fleet sweep: **an incident log written from server logs alone
will find the wrong start time.** The three sessions that detected this first left their
evidence in dispatch feedback and the agent-status channel, not in the error stream —
and those channels stayed healthy precisely *because* the fault was confined to the
Linear credential. Read the agents' own record before fixing a timeline from errors.
