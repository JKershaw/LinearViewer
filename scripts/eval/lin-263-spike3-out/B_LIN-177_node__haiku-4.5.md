## Reasoning

**Assessment:**
- Preparation: ✓ Complete — The plan in the description is thorough, surfaces are enumerated with dependency arrows, and the session-fit decision is explicit ("needs multiple sessions"). The re-grounding comments confirm Phase-2 foundation is in place and Phase-3 work is genuinely unstarted. No research gaps remain.
- Blockers: ✓ None — The blocking dependency (LIN-176) is resolved and verified. No `blocked` label present.
- Ready: ✓ Yes — Plan exists with clear surfaces (S0–S5), dependency arrows documented, and session-fit answer committed. The breakdown phase has already been completed (6 subtasks created with `blocked-by` relations). The task is now at the "pick a subtask and implement" stage.

**Signal Status:**
- Plan completion signals: ✓ Files enumerated, ✓ Approach documented, ✓ Testing strategy included, ✓ Session-fit answer committed ("needs multiple sessions"), ✓ Surfaces and arrows documented.
- Breakdown completion signals: ✓ 6 subtasks created (LIN-332 through LIN-337), ✓ `blocked-by` relations copied from plan arrows, ✓ Two parallelizable roots identified (S0 and S1).
- Current state: 2 subtasks done (LIN-333, LIN-332), 4 remaining. The task itself is "In Progress" and ready for implementation work to descend into a child.

→ **defer** — The honest next action lives in a child subtask, not at the parent node. The parent is a healthy container with a complete plan and decomposition; the real work is in the subtasks.

**Next:** The system will re-enter recommendation on the named child and descend until reaching the first actionable leaf task.

**DeferTo:** LIN-334

## Prompt

(No prompt generated for `defer` — routing to child task LIN-334.)